"""Configuration-space error diagnostics (spec section 6): per-joint
sweeps and random-batch statistics, computed against the analytic ground
truth without moving (or needing) the simulator. Pure Python + NumPy."""
